name: Docs

on:
  pull_request:
  push:
    branches: [main]

jobs:
  docs:
    name: Docs and specs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4.2.0
      - run: mise exec -- just docs-lint
